<% if user != current_user %>
  <% if current_user.followings?(user) %>
    <%= link_to 'Unfollow', followship_path(user),method: :delete, class: "btn-flat orange-text center" %>
  <% else %>
    <%= link_to 'Follow', followships_path(following_id: user), method: :post ,class: "btn-flat orange-text center"%>
  <% end %>
<% end %>
